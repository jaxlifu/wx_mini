<view class="page">
    <view class="page__hd">
        <text class="page__title" bindtap="showModal">{{text}}</text>
        <text class="page__desc" bindtap="showModal">{{text}}</text>
    </view>

    <view class="page__bd">
        <view class="section">
            <button bindtap="showModal">showModal</button>
        </view>
    </view>
</view>