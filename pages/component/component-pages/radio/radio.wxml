<view class="page">
    <view class="page__hd">
        <text class="page__title">{{text}}</text>
        <text class="page__desc">{{text}}</text>
    </view>

    <view class="section">
        <view class="section__title">当前选中的是: {{hero}}</view>
        <radio-group bindchange="bindchange">
            <label class="radio" wx:for="{{items}}">
          <radio value="{{item}}" />{{item}}
        </label>
        </radio-group>
    </view>
</view>